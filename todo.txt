#Displaying user flights:
-Javascript backend: live edit user booked flights? 

--cleanup: move all db funcitonality into a folder, move functions to helpers, document each file/function, move constants into a single file 
cleanup charts

--make bookedFlights update every hour? 

Linux todo: 
Check timestamps getting placed in DB 
Get Pipeline working w/ AWS creds 

#Many Fare Scraping

#bugs:
on lunux: 
--run user fares: don't run old dates

-debug dynamo db memory error 
-re-enable daily cron 
-maybe: transfer old mongo to new dynamo (only one weeks worth of data)

#Auto Check in


#Search



Setting up ec2 instance:
sudo yum install git
sudo yum install emacs
 sudo yum install make
 sudo yum install libffi-devel
 sudo yum install gcc
 sudo yum install openssl-devel
 sudo yum install -y gcc libxml2 libxml2-devel libxslt libxslt-devel
 sudo pip install scrapy
 sudo pip install boto3
 git clone https://github.com/chrislauinger/swa-scraper.git

 copy private file



